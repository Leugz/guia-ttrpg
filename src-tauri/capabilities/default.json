{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Enable the IPC bridge for the main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
